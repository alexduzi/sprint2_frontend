document.addEventListener('DOMContentLoaded', function() {
    console.log("Página de Notificações carregada!");

    // No futuro, aqui entrará a lógica para:
    // 1. Filtrar as notificações (Todas / Não Lidas) ao clicar nos botões.
    // 2. Marcar uma notificação como lida (removendo a classe 'unread') ao clicar nela.
    // 3. Carregar mais notificações ao rolar a página para baixo (lazy loading).
});